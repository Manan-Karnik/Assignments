# Get input from user
ip_str = input("Enter a string: ")

# Convert input to uppercase and lowercase
upper_string = ip_str.upper()
lower_string = ip_str.lower()

print("String in uppercase is '{0}'\nstring in lowercase is '{1}'".format(upper_string, lower_string))
